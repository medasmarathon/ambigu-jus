services:
  app:
    restart: always
    build: .
    ports:
      - 3000:3033
    volumes:
      - ambigu-data:/data/db
  mongo:
    image: mongo
    ports:
      - 27017:27017
    volumes:
      - mongodb:/data/db
volumes:
 ambigu-data:
